const assert = requier('assert')
var regCheck = function(regNum,location){
  var regcheck = regNum.startsWith(location)
  return check;
};
assert.equal(regCheck('DV 23 NB GP','GP'));
assert.equal(regCheck('DV 23 LP GP','MP'));
assert.equal(regCheck('CY 189-012','CY'));
assert.equal(regCheck('CY 189-012','CY'));
